#!/bin/bash 

OWN_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

source $1/bin/activate
export PYTHONPATH="$OWN_DIR/:$PYTHONPATH"
echo "python3 $OWN_DIR/zrlmpi_cc1.py ${@:2}"
python3 $OWN_DIR/zrlmpi_cc1.py ${@:2}
